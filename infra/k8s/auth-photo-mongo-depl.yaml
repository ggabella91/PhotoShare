apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-photo-mongo-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: auth-photo-mongo
  template:
    metadata:
      labels:
        app: auth-photo-mongo
    spec:
      containers:
        - name: auth-photo-mongo
          image: mongo
---
apiVersion: v1
kind: Service
metadata:
  name: auth-photo-mongo-srv
spec:
  selector:
    app: auth-photo-mongo
  ports:
    - name: db
      protocol: TCP
      port: 27017
      targetPort: 27017
